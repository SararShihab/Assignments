#include<stdio.h>
int main()
{
    int a, b;
    printf("Enter the lengths of two sides of the rectangele: ");
    scanf("%d %d", &a, &b);
    printf("The area of the ractangle is %d", a*b);
    return 0;
}